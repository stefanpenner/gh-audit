\* Vacuity check on the green bounds: Bait (~Compliant) must be
\* VIOLATED — TLC's witness trace is a reachable compliant state,
\* so EmptyCommit_green's Sound is not passing vacuously.
SPECIFICATION Spec
INVARIANTS Bait
CONSTANTS
    Variant = "linesfiles"
    MaxCount = 2
