\* Retired author-id-only rule (no signer anchor). The 2026-07 gap-hunt
\* headline: authorId is forgeable (noreply email → exempt id on an unsigned
\* commit), so TLC must find the forged-author waiver. The retired
\* verified_emails path is the same forgeable-identity class.
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    MaxCommits = 3
    Variant = "author"
