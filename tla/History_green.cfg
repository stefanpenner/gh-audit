\* Sound rule: every recorded prior head must remain reachable from the
\* current head (uses the auditor's retained memory of past heads). Sound holds.
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    Commits = {"c1", "c2", "c3"}
    MaxHeads = 4
    Variant = "recorded"
