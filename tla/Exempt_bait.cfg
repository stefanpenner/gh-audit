\* Vacuity check on the green bounds: Bait (~Compliant) must be VIOLATED —
\* the witness is a reachable compliant state (a signed exempt bot commit),
\* so Exempt_green's Sound is not passing vacuously.
SPECIFICATION Spec
INVARIANTS Bait
CONSTANTS
    MaxCommits = 3
    Variant = "signer"
