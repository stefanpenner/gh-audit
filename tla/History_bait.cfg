\* Vacuity check: Bait (~Compliant) must be VIOLATED — an honest
\* fast-forward-only sequence is compliant, so History_green's Sound is
\* not passing vacuously.
SPECIFICATION Spec
INVARIANTS Bait
CONSTANTS
    Commits = {"c1", "c2", "c3"}
    MaxHeads = 4
    Variant = "recorded"
