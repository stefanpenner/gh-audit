\* SHIPPED rule (prDelivers fails OPEN on unknown base). Expected to violate
\* Sound ONLY via the unknown-base state: an approved PR whose real base is a
\* sibling but whose base.ref is missing gets credited. This is the DOCUMENTED
\* accepted tradeoff (availability over strictness on legacy/missing data);
\* the counterexample names the exact residual assumption — unknown base must
\* never be attacker-suppressible. See tla/README.md.
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    NPRs = 2
    Variant = "failopen"
