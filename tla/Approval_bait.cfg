\* Vacuity check on the green bounds: Bait (~Compliant) must be
\* VIOLATED — TLC's witness trace is a reachable compliant state,
\* so Approval_green's Sound is not passing vacuously.
SPECIFICATION Spec
INVARIANTS Bait
CONSTANTS
    MaxCommits = 4
    MaxTime = 2
    MaxReviews = 2
    CarveOut = "positional"
