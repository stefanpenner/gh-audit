\* Shipped SOUND rule: signing_policy=required — exempt only on a verified
\* signer whose committer id is on the exempt list. Sound must hold.
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    MaxCommits = 3
    Variant = "signer"
