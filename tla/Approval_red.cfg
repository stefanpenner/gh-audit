\* Retired rule: committer-timestamp carve-out.
\* Expected: TLC finds the backdated GIT_COMMITTER_DATE laundering attack
\* (invariant Sound violated) — this config documents the closed hole.
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    MaxCommits = 4
    MaxTime = 2
    MaxReviews = 2
    CarveOut = "timestamp"
