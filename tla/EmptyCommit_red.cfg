\* Naive rule: zero lines only. TLC must find the rename-only laundering
\* (0 lines, >=1 file, not really empty, waived compliant).
SPECIFICATION Spec
INVARIANTS TypeOK Sound
CONSTANTS
    Variant = "lines"
    MaxCount = 2
